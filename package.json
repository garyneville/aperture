{
  "name": "@home/photo-brief",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --loader ts-node/esm workflow/assemble.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:workflow": "npm run build && git diff --exit-code -- ../../ansible/files/n8n-workflows/photography-weather-brief.json"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
